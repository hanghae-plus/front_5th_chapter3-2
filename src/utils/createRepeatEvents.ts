export const createRepeatEvents = (event: Event) => {
  const { repeat, endDate } = event;
  const { type, interval, endDate } = repeat;
  const { date, startTime, endTime, description, location, category } = event;
};
