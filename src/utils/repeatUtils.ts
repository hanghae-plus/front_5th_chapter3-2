export const getRepeatDates = (
  today: Date,
  repeatOptions: { type: string; interval: number; endDate?: Date; count?: number }
) => {
  console.log('Date', today);
  console.log('repeatOptions', repeatOptions);
  return [];
};
