import { act } from '@testing-library/react';
import { renderHook } from '@testing-library/react-hooks';

import { useRecurringEvents } from '../../hooks/useRecurringEvents';

describe('반복 일정 관련 Hook (useRecurringEvents)', () => {
  // 반복 유형 선택 테스트
  it('반복 유형이 선택되지 않았을 때 기본값은 "none"이어야 한다', () => {});

  it('반복 유형을 "매일"로 변경할 수 있다', () => {});

  it('반복 유형을 "매주"로 변경할 수 있다', () => {});

  it('반복 유형을 "매월"로 변경할 수 있다', () => {});

  it('반복 유형을 "매년"로 변경할 수 있다', () => {});

  // 반복 간격 설정 테스트
  it('반복 간격이 설정되지 않았을 때 기본값은 1이어야 한다', () => {});

  it('유효하지 않은 반복 간격 값(0 이하)을 설정하면 오류가 발생해야 한다', () => {});

  it('반복 간격을 변경할 수 있다', () => {});

  // 반복 종료 테스트
  it('반복 종료일이 설정되지 않았을 때 2025-09-30까지로 반복 설정되어야 한다', () => {});

  it('반복 종료일이 시작일보다 이전이면 오류가 발생해야 한다', () => {});

  it('반복 종료일을 설정할 수 있다', () => {});

  // 반복 일정 변경 테스트
  it('단일 반복 일정을 수정하면 해당 일정만 수정되고 반복 속성이 제거되어야 한다', () => {});

  // 반복 일정 삭제 테스트
  it('단일 반복 일정을 삭제하면 해당 일정만 삭제되어야 한다', () => {});
});
